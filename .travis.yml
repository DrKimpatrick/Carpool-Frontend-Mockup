language: python
python:
   "3.6.3"
install:
    "pip install -r requirements.txt"
script:
    - py.test
    - nosetests --with-coverage --cover-erase --cover-package=run.py && coverage report
after_success:
- "coveralls"
